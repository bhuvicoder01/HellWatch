'use client';

// This component is no longer needed as CSS handles pull-to-refresh prevention
// The overscroll-behavior-y: none in globals.css prevents pull-to-refresh

export default function PreventPullToRefresh() {
  return null; // This component doesn't render anything
}